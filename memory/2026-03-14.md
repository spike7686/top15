# 2026-03-14

## TOP15 系统本轮任务记录

### 本轮目标
围绕 TOP15 分析系统，完成从基础排行抓取到结构分析、黑马筛选、前端解释展示的一整套闭环。

### 已完成的关键决策与变更

#### 1. 时间口径修正
- 快照时间不再默认裸显示 UTC。
- 采集脚本新增 `captured_at_cst`。
- 前端优先显示北京时间（GMT+8）。

#### 2. 稳定币过滤
- 排行与分析默认剔除稳定币。
- 包括 USDT、USDC 以及一组常见稳定币 symbol/name 规则。

#### 3. 成交额门槛调整
- 原规则：24h 成交额 > 1.5 亿美元。
- 新规则：24h 成交额 > 1500 万美元。
- 后续全部分析默认按新规则进行。

#### 4. 旧数据处理策略
- 未直接删除旧规则历史。
- 采用 B 方案：归档旧规则数据。
- 归档目录：`data/top15_tracker/archive/rule_gt_150m_before_20260314/`
- 当前主历史文件仅保留新规则口径。

#### 5. Binance 现货约束
- 再次收紧规则：只保留 Binance 现货可交易标的进入 TOP15。
- 当前正式规则：
  - 24h 成交额 > 1500 万美元
  - 剔除稳定币
  - 仅 Binance 现货可交易标的
- 当前 `filter_rule`：`24h_volume_usd_gt_15m_excluding_stablecoins_binance_spot_only`

#### 6. K线历史数据接入
- 数据源：Binance
- 周期：`1h / 4h / 1d`
- 窗口：最近 7 天
- 存储策略：按 `open_time_ms` 增量去重，只补新K线，避免重复写入和过度请求
- 存储目录：`data/top15_tracker/klines/<SYMBOL>/<interval>/candles.csv`
- 元数据目录：`data/top15_tracker/meta/klines/<SYMBOL>__<interval>.json`

#### 7. 结构特征层完成
当前默认结构特征字段：
- `trend_1d`
- `trend_4h`
- `trend_1h`
- `breakout_1h`
- `breakout_4h`
- `price_change_7d_pct`
- `price_change_4h_window_pct`
- `price_change_1h_window_pct`
- `max_drawdown_7d_pct`
- `structure_score`
- `structure_grade`
- `structure_state`

结构特征层定位：回答“走势结构是否健康、是否具备趋势支撑”。

#### 8. 黑马筛选层完成
当前默认黑马字段：
- `darkhorse_score`
- `top5_potential`
- `sustainability`
- `risk_reward_profile`
- `darkhorse_tag`

黑马筛选层定位：回答“是否值得重点跟踪、是否有冲前五潜力、持续性如何”。

#### 9. 前端展示完成
前端已改为独立展示以下区域：
- 数据层
- 特征层
- 证据链总结
- 最终结论
- 黑马候选列表
- 算法详情按钮（Markdown 风格）

#### 10. 前端额外要求完成
- 黑马候选列表已加入价格信息。
- 页面算法详情按钮可点击展开 Markdown 风格算法说明。
- 页面说明文案已同步为“仅 Binance 现货”。

### 当前系统状态
- 网页已确认 OK。
- 当前分析默认使用最新规则，不再默认使用旧归档数据。
- 下次若继续分析，应直接基于这套现成结构继续推进，而不是重新搭基础设施。

### 下次延续时的默认认知
如果宝总没有重新改规则，下次默认应知道：
1. TOP15 不是泛市场榜单，而是“新规则 + Binance 现货约束”的榜单。
2. K线、结构特征、黑马筛选都已经接好。
3. 前端也已经具备可解释展示能力。
4. 后续应更多做模型优化、证据提炼、回测和候选判断，而不是重复搭底层。

## 追加记录（持续走强筛选层）
- 已新增“持续走强筛选层”，字段包括：`persistence_score`、`persistence_label`、`continuation_risk`、`continuation_evidence`。
- 持续走强层已接入采集主流程，并已写入 `latest/latest.json`、`latest/latest.csv`、`display/latest_display.json`、`display/latest_display.csv`。
- 持续走强层逻辑核心：趋势一致性（1d/4h/1h）、回撤控制、活跃承接、突破状态、过热惩罚、验证强度。
- 前端当前状态：持续走强字段已经接到黑马候选卡片、证据链总结、算法详情 Markdown 中；但尚未做成单独的“持续走强候选榜/专区”。
- 如无新指令，下次继续时应默认知道：持续走强层已经有数据和基础展示，下一步若继续前端优化，优先做独立榜单与排序视图。

## 追加记录（二：时间确认层 + 交叉确认层 + 三榜前端）
- 已新增 TOP15 时间确认层，当前默认字段包括：`top15_persistence_hours`、`top15_presence_ratio_24h`、`top15_presence_snapshots_24h`、`top15_snapshot_count_24h`、`top15_observation_window_hours`、`top15_snapshot_interval_hours_est`。
- 当前时间确认采用最近 24 小时历史快照推导，并以 1 小时作为交叉确认基础门槛。
- 已新增交叉确认层，当前默认字段包括：`overlap_gate_pass`、`overlap_score`、`overlap_label`、`overlap_rank_signal`、`overlap_candidate`、`overlap_evidence`。
- 交叉确认层定位：综合黑马分、持续走强分、在榜持续时长、在榜占比、趋势状态与风险项，筛出更高质量的趋势候选。
- 交叉确认当前基础门槛：`darkhorse_score >= 7`、`persistence_score >= 5`、`top15_persistence_hours >= 1`、`continuation_risk != High`、`trend_1d / trend_4h` 不转弱。
- 前端首页现已升级为三榜结构：`黑马候选榜 / 持续走强候选榜 / 交叉确认候选榜`。
- 交叉确认榜当前已采用“方案 B”：
  - 正式交叉候选：只显示 `overlap_candidate = true`
  - 观察中：显示高分但尚未通过最终门槛的样本
- 三个榜单已完成不同科技色系分区：
  - 黑马榜：蓝紫系
  - 持续走强榜：青绿系
  - 交叉确认榜：金橙系
- 三个榜单的卡片也已跟随各自主题色，且都加了轻量顶部状态条 / 细线高光。

## 当前产品代码位置与启动方式（给未来自己）
- 主采集与分析脚本：`scripts/top15_collector.py`
- 前端页面目录：`apps/top15-frontend-v2/`
  - `index.html`
  - `app.js`
  - `styles.css`
- 只读后端服务：`services/top15-readonly-backend-v2/server.py`
- 产品说明文档：`TOP15_TRACKER.md`

### 当前启动方式
1. 更新数据：
   - `python3 scripts/top15_collector.py`
2. 启动只读后端：
   - `python3 services/top15-readonly-backend-v2/server.py`
3. 浏览器访问：
   - `http://127.0.0.1:8080/`

### 当前只读后端接口
- `/api/health`
- `/api/manifest`
- `/api/latest-display`
- `/api/latest-analysis`
- `/api/latest-summary`
- `/api/snapshots`
