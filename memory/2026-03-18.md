# 2026-03-18

- TOP15 通知链路修复已完成并验证：`scripts/top15_collector.py` 的 `load_recent_top15_history()` 已改为以 `data/top15_tracker/history.jsonl` 作为 persistence / 连续历史真相源，避免 `history.csv` 污染 `top15_persistence_hours` 等关键字段计算。
- Feishu 自动通知策略已固定为“每小时都发”，不再依赖“是否存在正式交叉候选”作为发送前提；现有 cron 为 `3 * * * *`，webhook 已升级为交互式卡片，手动测试成功。
- 第一批路径研究字段已接入 `scripts/top15_collector.py` 并跑通：`was_in_top15_15m_ago / 30m / 60m`、`rank_change_15m / 30m / 60m`、`darkhorse_score_delta_30m`、`persistence_score_delta_30m`、`overlap_score_delta_30m`。
- 第二批时间路径字段已接入并验证：`current_episode_start_utc / cst`、`current_episode_duration_hours`、`hours_to_top10`、`hours_to_top5`、`hours_to_top3`、`entry_to_confirmation_hours`。这些字段已落到 `latest/latest.*`、`history.csv`、`history.jsonl`、`display/latest_display.*`。
- 当前路径研究已形成第一版结果文件：`analysis/top15_path_research_v1.md` 与 `analysis/top15_path_research_v1.json`。
- 路径研究 V1 的关键结论：很多强势币是“先快速冲进 TOP5 / TOP3，再经过更长时间才完成正式交叉确认”；因此 `overlap_candidate` 更适合作为“持续性确认信号”，不是“瞬时启动识别信号”。
- 当前正式交叉候选样本路径统计（基于当时最新快照）显示：`median_hours_to_top10 = 0.0h`、`median_hours_to_top5 = 0.1275h`、`median_hours_to_top3 = 0.626h`、`median_entry_to_confirmation_hours = 1.446h`、`median_episode_duration_h = 3.088h`。
- ENJ / ANKR 是当前最典型的“快推进、慢确认”样本：ENJ `entry_to_confirmation_hours ≈ 17.539h`，ANKR `≈ 9.544h`；AUCTION 也呈现类似但更短的滞后。CHR / HOT / VANRY 更接近“快推进、较快确认”或混合路径。
- 本轮研究后形成的重要方法论决策：后续模型应拆成两条线研究——A）更早识别潜在黑马的“启动前识别模型”；B）筛选高确定性的“持续性正式确认模型”。不要把“榜位推进速度”和“正式确认速度”混成一个指标体系。
- 当前数据量仍只足以支持探索性方法与字段设计，不足以固化稳定阈值或宣称 ENJ / ANKR 已产出“确定性规则”；后续仍需 2~4 周稳定历史继续验证。
